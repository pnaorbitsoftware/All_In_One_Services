# Public Expo values only. Server secrets belong in backend/.env.
EXPO_PUBLIC_API_URL=http://10.0.2.2:5000/api
EXPO_PUBLIC_APP_LINK=https://your-frontend-domain.com
# Tracking currently uses location cards plus an external Google Maps link.
# No map key is required unless an embedded map provider is added later.
